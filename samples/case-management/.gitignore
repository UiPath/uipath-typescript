# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# UiPath coded app local config
.uipath/
.env

# Repo-root .gitignore ignores `lib/` (SDK build output) — re-include this
# sample app's own source lib directory so it is committed.
!/src/lib/
!/src/lib/**

# Real config (copied from uipath.json.example, filled with your org/tenant/clientId)
uipath.json
