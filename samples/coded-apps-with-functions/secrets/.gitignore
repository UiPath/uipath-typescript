node_modules
dist
dist-ssr
*.local
.uipath
*.nupkg

# Solution pack output
out/

# The repo root ignores `lib/` as an SDK build output; here it is hand-written source.
!secrets-functions/lib/
!secrets-functions/lib/**

# Local OAuth config for `npm run dev` — copy from uipath.json.example.
# The functions project's uipath.json is its manifest and must stay tracked.
secrets-app/source/uipath.json

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.env
