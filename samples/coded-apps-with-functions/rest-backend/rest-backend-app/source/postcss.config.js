// Apollo Wind ships a PostCSS config (Tailwind v4 + autoprefixer) — reuse it
// directly so this sample stays in lockstep with the design system.
import apolloWindPostcss from '@uipath/apollo-wind/postcss'

export default apolloWindPostcss
