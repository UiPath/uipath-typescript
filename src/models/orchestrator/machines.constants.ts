/**
 * Maps fields for Machine entities to ensure consistent naming.
 * Currently no field renames are needed as Machine API fields
 * already align with SDK naming conventions.
 */
export const MachineMap: { [key: string]: string } = {};
