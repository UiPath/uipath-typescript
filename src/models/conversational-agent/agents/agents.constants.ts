/**
 * Constants for Agent Service
 */

import { CommonFieldMap } from '../common.constants';

/**
 * Maps fields for Agent entities to ensure consistent SDK naming
 */
export const AgentMap: { [key: string]: string } = {
  ...CommonFieldMap
};
