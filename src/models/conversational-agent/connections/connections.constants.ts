/**
 * Constants for Connection Service
 */

/**
 * Maps API field names to SDK field names for connection auth responses
 */
export const ConnectionAuthMap: { [key: string]: string } = {
  expiresAt: 'expiresTime',
};
