/**
 * Maps Document Understanding validation operation fields to SDK names.
 * Applied after PascalCase keys are converted to camelCase.
 */
export const DuValidationMap: { [key: string]: string } = {
  createdAt: 'createdTime',
  lastUpdatedAt: 'lastModifiedTime',
};
